mod schema;
mod model;
mod api;
mod repository;

use actix_web::{App, HttpServer};
use std::io::Result;

/**
 * main function, it starts the backend server
 */
#[actix_web::main]
async fn main() {
    
}
